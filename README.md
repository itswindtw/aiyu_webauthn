# Lokto_webauthn

An OCaml webauthn implementation from scratch.

## Develop

```bash
$ dune build -w
$ dune runtest -w
```

## Example

A demo project built with dream framework is included.

```bash
$ dune exec example/server.exe
```
